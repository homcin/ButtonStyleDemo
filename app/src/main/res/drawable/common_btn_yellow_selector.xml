<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按压时 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <padding
                android:top="@dimen/offset_12dp"
                android:bottom="@dimen/offset_12dp" />
            <corners android:radius="@dimen/corner_radius_8dp" />
            <stroke
                android:width="@dimen/stroke_width_1dp"
                android:color="@color/stroke_yellow"/>
            <gradient
                android:angle="-90"
                android:startColor="@color/pressed_yellow_dark"
                android:endColor="@color/pressed_yellow_light" />
        </shape>
    </item>
    <!-- 默认时 -->
    <item>
        <shape android:shape="rectangle">
            <padding
                android:top="@dimen/offset_12dp"
                android:bottom="@dimen/offset_12dp" />
            <corners android:radius="@dimen/corner_radius_8dp" />
            <stroke
                android:width="@dimen/stroke_width_1dp"
                android:color="@color/stroke_yellow"/>
            <gradient
                android:angle="-90"
                android:startColor="@color/yellow_light"
                android:endColor="@color/yellow_dark" />
        </shape>
    </item>
</selector>